#pyqt install commands (Type in cmd):

    pip install pyqt5-tools
    pip install pyqt5
    
#Extract exe file from pyinstaller (Type in cmd):

   pip install pyinstaller
   
#Once extracted.....go to the extracted path >> dist >> main >> find program <filename.exe>

#Run the exe file
